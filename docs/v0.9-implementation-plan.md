# RLR v0.9 Implementation Status

最后更新：2026-07-30

## 范围冻结

已纳入：`v2.1-catalog-1` profile、YAML persona catalog、L8 profile-aware artifact、L9a→L9b 串行、Curie L1/L4 pre-research receipt、精确 evidence run binding、ContextManifest/v2、RunReceipt/v1、candidate-scoped Obsidian/report consumer。

已移出并后置：hypothesis dashboard、literature-source MCP、PaSa、完整 AI4AI 治理、v2.0 清理。→ 见 `未来方向-AI4AI-PaSa-v21-MCP.md`。

版本策略：v0.9 过渡收尾；下一个版本直接命名 v1.0。v1.0 起删除 v2.0 compatibility profile、migration、legacy read path。

## 当前状态

| 项目                                             | 状态                 |
| ---------------------------------------------- | ------------------ |
| RunReceipt/v1、ContextManifest/v2、provider 哈希绑定 | ✅ 完成               |
| 三 profile 显式解析；新项目默认 v2.1-catalog-1            | ✅ 完成               |
| Profile-aware L8（Tukey/Curie 按 profile）        | ✅ 完成               |
| L9 串行门（L9b 等待 finalized L9a）                   | ✅ 完成               |
| EvidenceRunReceipt/v1.1 精确绑定 L1/L4/L8.5        | ✅ 完成               |
| YAML persona catalog + 严格 validator            | ✅ 完成               |
| Profile-aware runner/report/Obsidian/L8.5 路径   | ✅ 完成               |
| CLI check-deps NameError（engine 猴补丁）           | ✅ FIXED 2026-07-30 |
| Deep-research 宿主一致性门                           | ✅ FIXED 2026-07-30 |
| **文献候选逐条过滤规则**                                 | ✅ FIXED（`deep_research.py:333` + `persist_run:421`） |
| runner 不再默认 Codex                                | ✅ FIXED 2026-07-31（`run_loop.py` `DEFAULT_CONFIG`） |
| mixed runtime spec fail-closed                    | ✅ FIXED 2026-07-31（`validate_spec_consistency()`） |
| unknown host 不再静默放行                              | ✅ FIXED 2026-07-31（`host_matches(explicit=...)` + `RLR_HOST_BACKEND`） |
| 跨平台 sentinel 测试（子进程从未启动的证明）                     | ✅ FIXED 2026-07-31（`tests/test_deep_research.py`） |
| 最小跨平台 CI                                         | ✅ FIXED 2026-07-31（`.github/workflows/ci.yml`） |
| **覆盖率**（当前 64%，口径 `--cov=src`）                  | 📊 记录项，非发布硬门（见下）|

## 发布门 Checklist

- [x] 全量测试通过 — `404 passed, 0 failed`（证据：`docs/releases/v0.9-verification.md` 「v0.9 收尾三批验证记录」）
- [x] runner 不再默认写死 Codex backend（证据：`src/run_loop.py` `DEFAULT_CONFIG`、`ensure_pre_research()`）
- [x] mixed runtime spec（backend 与 executable/skill_path/plugin_dir 矛盾）fail-closed（证据：`src/research_loop/deep_research.py` `validate_spec_consistency()`；测试见 `tests/test_deep_research.py`）
- [x] unknown host 不再静默放行，需 `RLR_HOST_BACKEND` 或 `--backend` 显式声明（证据：`src/research_loop/deep_research.py` `host_matches()`/`detect_host_backend()`）
- [x] 关键 guard（宿主不一致、spec 矛盾、unknown host）均有 sentinel 测试证明子进程从未启动，而非仅 exit code 匹配（证据：`tests/test_deep_research.py`）
- [x] 文献候选逐条过滤 + rejected-record audit（证据：`deep_research.py:333`、`persist_run:421/:479`）
- [x] CLI check-deps NameError 修复，standalone import 无需 `engine.py`（证据：`tests/test_cli_standalone_imports.py`）
- [x] 最小跨平台 CI（ubuntu/windows × 3.11/3.12，覆盖率仅记录不设 `--cov-fail-under`）（证据：`.github/workflows/ci.yml`）
- [ ] 覆盖率达到 80% — **不再是发布门**，v0.9 已将其降级为记录项（当前 64%，`--cov=src`）
- [ ] `claim >-` YAML 解析 bug — 独立未决项，本次三批未处理
- [ ] Codex 环境标记（`_HOST_MARKERS`）在真实 Codex 会话中验证 — 未处理，`RLR_HOST_BACKEND` 是当前显式声明的临时替代，不是标记探测本身

**v0.9 仅为双宿主（codex/claude）安全收尾，不是最终跨平台架构；adapter registry、MCP research transport、AI4AI 治理层统一进入 v1.0。**

## 覆盖率现状（记录项，非发布门）

完整套件 404 passed，覆盖率 64%（`--cov=src`）。ledger commands 37%、lifecycle 29%。v0.9 不要求达到 80%；该数字仅供 v1.0 规划参考，不得通过 coverage omit、空测试或复制测试人为拉高。

## 已修复缺陷（供参考）

**CLI check-deps NameError（FIXED 2026-07-30）：** `REQUIRED_DEPENDENCIES` 所有权移入 `common.py`，删除 `engine.py` 猴补丁，新增 `tests/test_cli_standalone_imports.py`（5 passed）。

**Deep-research 宿主一致性（FIXED 2026-07-30）：** `detect_host_backend()`、`host_matches()`、`default_runtime_config()` 按宿主派生 backend；`cmd_deep_research_run` 加宿主门（不一致 exit 3，`--allow-host-mismatch` 放行）。Codex env 标记未验证，Codex 会话须显式 `--backend codex`。

**文献候选逐条过滤（FIXED）：** `_filter_unidentifiable_papers()`（`deep_research.py:333`）对每条论文独立检查 DOI/PMID/稳定 URL，缺失记录进入 `rejected_papers` 审计列表并写入 run artifact（`persist_run:421`、`:479`），全部不合格时 `DeepResearchError` fail-closed。测试见 `tests/test_deep_research.py:110`。

**runner 默认 Codex（FIXED 2026-07-31）：** `run_loop.py` 的 `DEFAULT_CONFIG` 不再写死 `backend: codex`；`ensure_pre_research()` 在 runner 未显式 override 时不传 `--backend`，由 `deep-research-run` 落回项目 `00_Preflight/deep_research_runtime.json`。

**mixed runtime spec（FIXED 2026-07-31）：** 新增 `validate_spec_consistency()`，拒绝 backend 与 executable/skill_path/plugin_dir 矛盾的 spec，接在 `cmd_deep_research_run` 的宿主门之后。

**unknown host 静默放行（FIXED 2026-07-31）：** `host_matches()` 新增 `explicit` 参数，未探测到宿主且非 explicit 时 fail-closed；`detect_host_backend()` 先读 `RLR_HOST_BACKEND`（值需在 `SUPPORTED_BACKENDS` 内），再回落 marker 探测。此前未探测到宿主会静默继承项目 runtime 文件的 backend，属于同一类配额误用风险。

**sentinel 测试（新增 2026-07-31）：** `executable=sys.executable` + 工作目录下名为 `exec` 的脚本，跨平台证明「guard 拦截时子进程从未启动」，而非仅比对 exit code/stderr 文本。覆盖宿主不一致、spec 矛盾、unknown host 三条 guard。

**最小跨平台 CI（新增 2026-07-31）：** `.github/workflows/ci.yml`，矩阵 ubuntu/windows × 3.11/3.12；覆盖率仅记录，不设 `--cov-fail-under`。

## 后续顺序

1. v0.9 正式发布后，literature-source MCP 作为 discovery-only 影子检索
2. 影子基准通过后评估 PaSa 查询扩展/Crawler/Selector
3. 完整 AI4AI 治理、dashboard 与 v2.0 移除分别建立独立计划
4. `claim >-` 解析 bug、覆盖率提升、Codex 环境标记验证，作为独立收尾项排期（均非 v0.9 发布门）
