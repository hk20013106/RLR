# PR #12 L4A to L4B contract closeout

## Goal

Close the native L4A to L4B contract from `8676c217df92916c3a621a2733c15c926f08e84e`, preserving the frozen-corpus, evidence, Methods, hash, anchor, and runtime-observability gates. Only a fresh final native L4A to L4B pilot may use a real provider, and it runs once after all tests and CI are green.

## Work sequence

1. Audit the resume, ordinary L4, inventory, resolver, and provider-runtime paths; record the exact legacy/native boundary.
2. Add regression tests for legacy early rejection, native inventory completeness, resume identity and frozen-corpus integrity, observability ownership, and the ordinary L4A to L4B handoff. Run the new tests and capture the RED failures before changing production code.
3. Implement the smallest strict validation and accounting changes. Run targeted provider-runtime, resolver/resume, and L4/L4.5 tests, then the full suite and `git diff --check`.
4. Commit and push the verified code, update PR #12, wait for CI, and do not start the real pilot until the required checks are green.
5. Create a new pilot project and run exactly one native L4A provider invocation followed by one L4B resolver invocation. Inspect the manifest, frozen corpus, evidence/gaps, retrieval receipts, observability artifacts, audit, and process cleanup. Do not run L4C or L4.5.
6. Merge PR #12 only if all tests, CI, and the single final pilot pass; otherwise stop with the precise failure and do not retry the provider.

## Completion evidence

- Native v2 manifests have a non-empty valid method inventory and every method has exactly one evidence or explicit gap outcome.
- Legacy manifests fail before any L4B artifact or provider invocation.
- Resume validates path, persisted bytes, SHA, candidate/project identity, schema, inventory, selected assets, and frozen-corpus immutability.
- Ordinary native L4 remains L4A then frozen handoff then L4B.
- `l4_inventory` and all other provider owners use the PR #14 observability proxy.
- Resolver gates for source bytes, source hash, receipt, contiguous Methods text, minimum 500 bytes, identity, and anchors remain fail-closed.
