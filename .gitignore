# Python
__pycache__/
*.pyc

# Temporary scripts (build helpers)
_*.py
_*.ps1

# Recovery / temp
_recovery/
.tmp-local/

# Obsidian vault (local copy, not for git)
# (synced copies live in the actual vault)

# OS
Thumbs.db
desktop.ini
.DS_Store
_tmp_*.json

# --- Generated RLR project RUN DATA (output, NOT source) ---
# Live research projects are runtime output (deltas, run-receipts, audit
# manifests, reports, decision logs, turing workspaces, literature DB).
# Keep them on disk but OUT of the source repo. DemoProject_v03 is kept as the
# tracked example.
Yigene_WGCNA_v03/
Yigene_Enhancer_v04/

# Stray run artifacts accidentally written to the repo root
scripts_wgcna_loop/
nul
_codex_write_probe.txt
*.bak
_*_delta.json
_tmp_lines.txt

# DemoProject generated RUN DATA (the demo is tracked, its outputs are not)
DemoProject_v03/08_Audit/
DemoProject_v03/10_Pitfall_Ledger/
